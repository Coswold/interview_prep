"""Q3.1"""

class MultiStack(object):

    def __init__(self, size=1), count=3:
        self.stack_capacity = size
        self.stack_count = count
        self.stack = [None] * size * count
        self.stack1 = 0
        self.stack2 = len(self.stack) // count
        self.stack3 = (2 * len(self.stack)) // count


if __name__ == '__main__':
    three_in_one(stack)
